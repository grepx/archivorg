package gregpearce.archivorg.platform.feed;

import android.support.v7.widget.RecyclerView;
import android.view.View;

public class FeedLoadingViewHolder extends RecyclerView.ViewHolder {
  public FeedLoadingViewHolder(View itemView) {
    super(itemView);
  }
}