<resources>
  <!-- APP THEME STYLES -->
  <style name="AppTheme" parent="AppTheme.Base.Overrides"/>

  <style name="AppTheme.Base" parent="Theme.AppCompat.DayNight.NoActionBar">
    <item name="colorPrimary">@color/colorPrimary</item>
    <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
    <item name="colorAccent">@color/colorAccentLight</item>
  </style>

  <style name="AppTheme.Base.Overrides">
    <!-- Higher API levels can override here -->
  </style>

  <!-- ACTION BAR STYLES -->
  <style name="ActionBar" parent="ActionBar.Base.Overrides"/>

  <style name="ActionBar.Base" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
  </style>

  <style name="ActionBar.Base.Overrides">
    <!-- Higher API levels can override here -->
  </style>
</resources>
